name: Push builds

on:
  push:
    branches:
      - gd4
      - godot-3.x
      - main

jobs:
  build-web-godot-3:
    if: ${{ github.ref_name == 'godot-3.x' }} 
    uses: Srynetix/godot-github-workflows/.github/workflows/build-game-4.0.2.yml@main
    with:
      platform: Web
      name: sxgd-kitchen-sink
      output_name: index.html

  build-web-main:
    if: ${{ github.ref_name == 'main' }} 
    uses: Srynetix/godot-github-workflows/.github/workflows/build-game-4.0.2.yml@main
    with:
      platform: Web
      name: sxgd-kitchen-sink
      output_name: index.html

  gd4-test:
    if: ${{ github.ref_name == 'gd4' }} 
    steps:
      - run: echo "hello"